{% extends 'base.html.twig' %}

{% block title %}A melhor feijoada{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    
<h1>Bem vindo ao Feijoada Week</h1>

<p>A primeira Feijoada Week foi um sucesso!</p>
<p>A segunda edição foi adiada devido ao coronga e assim que uma data for decidida, será informada.</p>
    
    {% for prato in pratos %}
        <h2>{{ prato.getNome|e }} - <a href="/restaurante/{{ prato.getRestaurante.getSlug|e }}">{{ prato.getRestaurante.getNome|e }}</a></h2>
        <h3>{{ prato.getData|date("d/m/Y") }}</h3>
        <p>{{ prato.getDescricao|raw }} </p>
        <p>{{ prato.getVideo|raw }} </p>
    {% endfor %}
    
</div>
{% endblock %}
